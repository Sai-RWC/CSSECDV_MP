<div class="flex flex-col items-center justify-center min-h-screen">
    <div class="bg-white rounded-2xl shadow-md p-10 flex flex-col items-center space-y-4 max-w-md w-full">
        <h1 class="text-6xl font-bold text-emerald-700">{{statusCode}}</h1>
        <h2 class="text-xl font-semibold text-gray-800">{{title}}</h2>
        <p class="text-gray-500 text-center">{{message}}</p>
        <a href="/login" class="mt-4 px-6 py-2 rounded-full bg-emerald-700 text-white font-bold
                                hover:scale-105 transition-transform duration-200 shadow-md">
            Go back
        </a>
    </div>
</div>
